## 第2回 summary

### 概要
- 2026.1.16 at 13:30〜14:40 オンライン

- 参加者数 9（主催 1, 全体参加 7, 部分参加 1）

- 録画あり（120日有効）

<br>

### 内容

- (1) 作業環境フォロー

  - GitHub：アカウント作成〜このリポジトリを開けるまで

  - Googleドライブ：この学習会用のフォルダを作って開く

  - Colab：↑ の学習会用フォルダから右クリックで開く

<br>

- (2) Colabで「数当てられ」ゲームを作ろう（本のChapter 1）

    > 1人の参加者が画面共有し、説明を受けながら作業<br>
    他の参加者は各自作業

  - 新しいノートブックを作成

  - 本にある以下のコードを、一つのセルに順々に足す

    - 本にある赤字＝書き足すコード、黒字＝既に書いてあるはずのコード

    - `1ｰ3-1` , `1ｰ3-2`（p.23）


    - `1ｰ3-4`（p.27）

    - `1ｰ3-5`（p.28）

    - `1ｰ3-6`（p.30）

    - `1ｰ3-7` , `1ｰ3-8`（p.33）

    - セルを実行してゲームができ、完成 \\^o^/

<br>

- (3) 作業の補足

  - 本にあるコードのうち `1-3-3`（p.26）は省略（すぐ後で消すので）

  - コードのうちコメント（`#` で始まる行）は時間節約のため入力しなくても可

  - コードに問題あれば<font color="red">赤字</font>や波下線で画面に示され、修正するとそれらの表示が消える

  - コードに問題ある状態で実行すると途中で止まり、エラー情報が結果表示欄に出る

    - エラー情報の最下部に直接の原因が表示されることが多い

  - インデント／スペースは本どおりでなくとも<u>コードが問題なく動けば</u>可

    - 例えば `=` の前後のスペースを省く等

    - 正統なインデント／スペースの入れ方をしたい人は本のコードどおりに

  - プログラム実行アイコン（黒丸に右向き三角）を押すと、黒丸の中が四角に変わる。実行中にこれを押すと中止できる

    - その場合（単なる実行中止）でも多くのエラー情報が表示される。これはColabの正常な挙動だが、初心者は戸惑うかも

  - 本にある用語説明等の解説は省き、各自の自習とした（質問等を次回受ける）

<br>

- (4) コードについての質疑応答

  - Q) `for i in range(7):` で数値0〜6をループしているが、数値の `+1` はどこで？

  - A) `range(7)` が連番のリストを生成しており、その中に含まれる

    - `for 変数 in リスト` で、リストの要素を先頭から取り出し変数に入れてループする

    - リストの説明は本のChapter 2 `p.46` を参照

- (5) クロージング

  - 次回の日時

    - 1週間後（1/23 Fri）の同時刻

  - 次回の内容：

    - (1) 今日のコードや本の用語説明等への質問

    - (2) 今日「時間が余ったら」で準備していた下記

      - プログラム変更 (1)

        - 現状：開始してすぐ「あなたの数字は50より大きいですか？」と聞かれる

        - 改良：開始してまず「数字を思い浮かべましたか？」と聞き `yes` が入力されたら質問を始める

      - プログラム変更 (2)

        - 確認：質問への答えで `yes` のつもりで `YES` を返したらどうなる？

        - 改良例 A：答えが大文字でも同じ結果にする

        - 改良例 B：答えが `yes/no` 以外なら「小文字のyesかnoで答えて下さい」と聞き直す

        - その他自分で改良例を考えてみる

      - 考察：今回のゲームを「誰でも遊べるWebアプリ」にするには？（時間あれば）

  - 本の準備について

    - 各自の作業中は見るページが各々異なるので、紙・電子問わず各自の手元にあるべき

    - 後々役に立つ内容があり学習会終了後も読めると望ましいが、そのためにはAmazon Kindle Unlimited等サブスクはずっと料金を払い続ける必要があり、結果的に高くつく

<br>

### 追記

- 終了後にあった質問：`if answer == 'yes':` は完全一致だが、部分一致にする方法は

- 答えの例

  - 先頭一致：if 文字列全体 `.startswith` (部分文字列) :

  - 末尾一致：if 文字列全体 `.endswith` (部分文字列) :

  - どこかに一致：if 部分文字列 `in` 文字列全体 :

    - 部分文字列が文字列全体に含まれる（先頭／末尾一致を含む）

    - 今回のコードにある `for i in ...` とは違う `in` の使い方

    - 文字列全体と部分文字列の順番が `startswith` `endswith` と逆

<br>

- コード例（セルに貼り付けて実行可）

  ```Python
  answer = 'y'
  if 'yes'.startswith(answer):
    print(answer, 'はyesに先頭一致します')
  else:
    print(answer, 'はyesに先頭一致しません')

  answer = 's'
  if 'yes'.endswith(answer):
    print(answer, 'はyesに末尾一致します')
  else:
    print(answer, 'はyesに末尾一致しません')

  answer = 'e'
  if answer in 'yes':
    print(answer, 'はyesのどこかに一致します')
  else:
    print(answer, 'はyesのどこにも一致しません')
  ```

<br>

- 他にも方法があります。気になる人は調べてみよう

<br>

---
